{
  "name": "wikiavec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./os-launch",
    "test": "mocha $(find $PWD -path '*os-*' -name '*-tests.js')",
    "dstart": "supervisor ./os-launch",
    "dtest": "mocha $(find $PWD -path '*os-*' -name '*-tests.js') -w --reporter min",
    "dtestdebug": "mocha $(find $PWD -path '*os-*' -name '*-tests.js') -w --reporter min -b"
  },
  "dependencies": {
    "basic-auth": "latest",
    "body-parser": "latest",
    "cookie-parser": "latest",
    "ejs": "latest",
    "express": "latest",
    "express-session": "latest",
    "mkdirp": "latest",
    "js-yaml": "latest",
    "underscore": "latest"
  }
}
